# Permissive no-op stand-in for zed's ztracing_macro (GPL-3.0), see vendor/ztracing.
[package]
name = "ztracing_macro"
version = "0.1.0"
edition = "2024"
publish = false
license = "Apache-2.0"
description = "No-op stub for zed's ztracing_macro"

[lib]
proc-macro = true
